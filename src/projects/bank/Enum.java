package projects.bank;

public class Enum {
    // This class is created to hold the enum AccountType
    public enum AccountType {
        // Enum constants options are the two types of accounts in this bank.
        SAVINGS, CHECKING
    }
}
